// Description of a Survey
export interface Survey {
    ID: number; 
    name: string;
}

// Description of a User
export interface User {
    ID: number;
    firstName: string;
    lastName: string; 
}